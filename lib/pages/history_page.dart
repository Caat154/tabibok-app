import 'package:flutter/material.dart';

class HistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('السجل')),
      body: Center(child: Text('هنا تظهر سجلاتك الصحية.')),
    );
  }
}
